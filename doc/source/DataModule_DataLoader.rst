The DataLoader submodule
========================

The DataLoader is a module for loading data objects from various
formats and provide them as Data objects. It also implements a generic
method *load* that tries to determine the type of the data to be
loaded from the extension of the file.

.. automodule:: natter.DataModule.DataLoader
   :members: load, matlab, nisdetDataObject, ascii, pydat, libsvm

